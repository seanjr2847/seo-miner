# seo-miner — 작업 규칙

## 화면을 건드리면 브라우저로 연다

`31/31 PASS` 는 "데이터가 맞다"는 뜻이지 "화면이 맞다"는 뜻이 아니었다. 2026-08-26
기준 최근 fix 8건이 전부 화면·문구·이음매였고, 파이썬 로직 버그는 한 건도 없었다.
전부 초록불 아래에서 나갔다.

`run_checks.py` 에 `test_render.py` 가 생겨(헤드리스 크롬) **열어만 봐도 아는 것**은
이제 자동으로 잡힌다: JS 가 터졌는지, 화면이 그려졌는지, URL 이 시킨 사이트가
열렸는지. 하지만 **눈으로 봐야 아는 것**은 여전히 검사에 안 나온다:

- 세로 메뉴가 가운데 정렬이라 스캔이 안 된다
- 날짜가 `20 / 26-08-21` 로 반토막 난다
- 문구가 결제 벽처럼 읽힌다
- 대비가 3:1 을 못 넘긴다

그래서: **화면 파일(`templates/`, `server/assets/`, `landing.html`, `app.html`)이나
사용자에게 보이는 문구를 고쳤으면, 커밋 전에 브라우저로 그 화면을 연다.** 고친
자리만 보지 말고 그 화면에 들어가는 경로를 실제로 눌러 본다 — 사이트 링크 hash
버그는 헤더만 보고 목록에서 들어가 보지 않아서 살아남았다.

호스팅 화면(`/d`)은 OAuth 때문에 그냥 못 띄운다. `test_render.py` 의 `serve()` 가
같은 조립본을 로컬에서 띄우는 방법을 갖고 있다 — 그걸 쓴다.

## 이음매는 양쪽 끝을 함께 검사한다

이 리포의 버그 상당수는 **파일 하나만 보면 어느 쪽도 멀쩡한** 종류다. 사이트 링크는
정상적인 링크였고 대시보드는 정상적으로 hash 를 읽었다. 어긋난 건 둘 사이다.

새 이음매를 만들면 `test_seams.py` 에 못 박는다. 지금 지키는 것:

1. 화면 id 는 페이로드에서 온다 (렌더된 글자를 정규식으로 되짚지 않는다)
2. 화면 목록의 정본은 뷰의 `view-def` 다 (`dash.html` 은 사본을 안 갖는다)
3. 셸은 한 벌이다 — 레일·화면 상자·전환은 원본(`dashboard.html` 의 `SM`)이 갖고,
   `dash.html` 은 `SM.addView`/`addSection` 으로 덧붙이기만 한다 (`!important` 금지)
4. 단계 용어표는 한 벌이다
5. 사이트 목록 링크가 hash 를 싣고, 대시보드가 그 hash 를 읽는다
6. 화면이 부르는 `/api/*` 가 그 화면이 뜨는 모든 서버에 있다
7. 화면에서 돌릴 수 있는 단계는 서버가 전부 받는다 (`run_all.VALID_STAGE_NAMES` 가 정본)
8. 기회 종류는 한 벌이다 — 만드는 쪽(`scoring.ALL_KINDS`)과 말하는 쪽(셸의
   `KIND_LABEL`·`PLAY`)을 양방향으로 대조한다
9. 화면이 부르는 `/capture X` 가 그 화면 `view-def` 의 `stages` 안에 있다
10. 원격 클라이언트(`remote.py` 와 그 `api()`/`fetch()` 를 쓰는 진입점)가 부르는
    `/api/*` 가 호스팅 서버에 전부 있다
11. 수집기가 전부 `remote.dispatch` 를 통과하고, 넘기는 단계 이름이 그 모듈이 맡은
    단계와 같다 (정본은 `run_all.STAGE_MODULES`)

검사를 추가했으면 **일부러 깨서 FAIL 나는 것까지 확인**한다. 안 그러면 통과하는
검사가 아니라 아무것도 안 보는 검사가 된다.

## 자간·등폭은 한글에 걸지 않는다

`text-transform:uppercase` 와 `letter-spacing` 은 **라틴 문자열 전용**이다. 한글에
걸면 `아 직 안 딴 기 회` 처럼 낱자가 흩어져 스캔이 안 된다. 이 리포는 화면·랜딩·
사이트 목록 세 곳에서 같은 실수를 각자 저질렀고, 애드온 두 개가 그걸 다시 자간
패치로 덮고 있었다 — 원인이 아니라 증상을 세 번 고친 셈이다.

- 마이크로 라벨(눈금·열 이름·배지)의 위계는 자간이 아니라 **크기·굵기·색**으로
  만든다. 정본은 `--micro` 토큰 한 벌이다.
- 트래킹이 필요한 자리(`SERP`, `RUNS`, `MOBILE vs DESKTOP`)에는 `.lat` 을 **손으로**
  붙인다. `:lang()` 으로는 못 가른다 — 한 요소 안에 한글과 라틴이 섞이기 때문이다.
- 등폭(`--mono`)은 **숫자·명령·URL·날짜·식별자**에만. 한글을 등폭으로 찍으면 폴백
  서체로 떨어져 자간이 벌어지고 획이 얇아진다.

## 목록·문구를 두 벌 만들지 않는다

이 리포가 반복해서 겪은 것: 같은 목록이 코드와 문서에 두 벌 있고 한쪽만 낡는다
(`9단계인데 5곳이 8단계라고 말한다`, `화면 목록 사본`). 개수·URL·단계 이름은
정본을 **가리키기만** 한다 — `run_all.py` 의 단계표, `doctor.py` 의
`CAPABILITIES`/`ALL_SKILLS`, 뷰의 `view-def`.

## 릴리스는 세 다리다

`.claude-plugin/plugin.json` version 범프 → push → 설치 캐시 갱신.
자세한 이유는 `docs/adr/0001-plugin-version-is-install-cache-signal.md`.

```
claude plugin marketplace update seo-miner
claude plugin update seo-miner@seo-miner     # 풀네임 필수
```

`server/` 만 바뀌었으면 플러그인 갱신은 필요 없다 — 반영은 Railway 자동 배포다.

## 검사

```
python run_checks.py            # 전부
python run_checks.py render     # 화면만
```
