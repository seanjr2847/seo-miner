---
description: seo-miner 환경 진단·온보딩 — 설치 직후 이것부터
argument-hint: "[doctor|web]"
---
setup 스킬(skills/setup/SKILL.md)을 읽고 워크플로우대로 수행하라. 반드시 doctor
스크립트 실행으로 시작해 진단 결과 기반으로만 안내하라. 인자에 `web`이 있으면
`python scripts/doctor.py --web`을 백그라운드로 띄워 설정 화면에서 직접 하게 하라:
$ARGUMENTS
