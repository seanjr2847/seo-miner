---
description: 기회를 리포의 실제 콘텐츠 변경으로 — profile/plan/run/status
argument-hint: "<profile|plan|run|status> <project> [...]"
---
create 스킬(skills/create/SKILL.md)을 읽고, 그 워크플로우에 따라 다음 요청을 수행하라.
발견 우선(프로필 없으면 profile부터), 발행 게이트(main 직행 금지, 브랜치+PR, [opp #id]
커밋), 루프 클로즈(createdb.py done)를 반드시 지켜라: $ARGUMENTS
